{
  "name": "harassment-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension for harassment detection (load unpacked).",
  "scripts": {
    "dev": "node scripts/dev.js"
  }
}
